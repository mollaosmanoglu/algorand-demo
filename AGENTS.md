<cli_tools>
- `playwright-cli`: for debugging frontend
- `gh`: Issues and PRs. `gh issue list`/`create`/`view`. Labels: `bug`, `feature`, `question`.
- `tvly`: Web search and research. `tvly search`, `tvly extract`, `tvly research`.
- `osascript`: Apple Calendar & Reminders via AppleScript. Calendar "Work" for events. Build dates programmatically (Dutch locale — string dates don't parse).
</cli_tools>

<commits>
- For issues and milestones, use the tags `feature`, `bug`, and `question`.
- Prefer lowercase, hyphenated branch names with intent: `feature-email-ingestion`, `fix-webhook-auth`, `docs-agent-guidance`.
- Conventional Commits: <type>(<scope>): <description>
- Types: feat, fix, refactor, chore, docs, test, build, ci, perf, style, revert
- Example: feat(agent): add transaction reconciliation tool
</commits>
    
